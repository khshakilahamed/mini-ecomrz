import Navbar from './Navbar';
import ContactHeader from './ui/ContactHeader';

const Header = () => {
      return (
            <div>
                  <ContactHeader/>
                  <Navbar/>
            </div>
      );
};

export default Header;